{
  "private": true,
  "scripts": {
    "postinstall": "dotnet tool restore",
    "start": "dotnet fable watch src -s -o build --noCache --run vite",
    "build": "dotnet fable src -o build --noCache --run vite build"
  },
  "devDependencies": {
    "vite": "^2.9.13"
  },
  "version": "1.0.0"
}
